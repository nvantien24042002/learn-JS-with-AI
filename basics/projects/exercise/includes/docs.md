1. Lý thuyết: includes() là gì?
includes() là một phương thức của Chuỗi (String) và Mảng (Array) trong JavaScript. Nhiệm vụ duy nhất của nó là trả lời câu hỏi: "Trong này có chứa cái này hay không?".
 *** Kết quả trả về: Luôn là true (nếu tìm thấy) hoặc false (nếu không tìm thấy).